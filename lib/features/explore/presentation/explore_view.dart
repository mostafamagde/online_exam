import 'package:flutter/cupertino.dart';

class ExploreView extends StatelessWidget {
  const ExploreView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(
          child:
          Text("Explore",style: TextStyle(fontSize: 50
          ),),
        )
      ],
    );
  }
}
